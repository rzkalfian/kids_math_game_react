export const TIMINGS = Object.freeze({
  ANSWER_FEEDBACK_MS: 1600,
  DRAG_ACTION_MS: 2000,
  ROLL_DELAY_MS: 450,
});

export const INITIAL_ANSWERS = Object.freeze({
  input: "",
  farmer: 0,
  fishing: 0,
  shooting: 0,
  space: 0,
});
